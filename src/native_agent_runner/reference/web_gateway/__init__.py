"""Reference implementation of the native-agent-runner web gateway contract.

Provided as an example web search/fetch/context gateway (and contract-test fixtures). Real
integrators are expected to build their own against ``native_agent_runner.contracts`` and
``docs/CONTRACTS.md``. Not part of the supported public surface.
"""

